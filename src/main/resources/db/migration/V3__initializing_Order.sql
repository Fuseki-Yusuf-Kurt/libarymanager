CREATE TABLE "order"
(
    "id"           INTEGER,
    "book_id"        INTEGER,
    "person_id"       INTEGER,
    "genre"        TEXT,
    "release_date" TEXT,
    "busy_date" TEXT,

    PRIMARY KEY ("id")
);